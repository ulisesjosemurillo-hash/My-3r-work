import sys

# Replace JtSurface with color #FFFFFF where it applies if we want exact 1-to-1 matching,
# but the reference already has #F7F5F0 as JtBackground and #FFFFFF as JtSurface.
# The code correctly implements this.
print("Theme verified.")
